# babbles-react-project

Project currently run on expo

run `npm install` to install packages

Install Expo Go on physical device

`npx create-expo-app project-name`

Start dev server by running `npx expo start`
